---
const { frontmatter } = Astro.props;
---

<main class="p-6 space-y-5">
  <h1 class="text-3xl text-center font-heading">{frontmatter.title}</h1>
  <p class="text-center"> {frontmatter.author}</p>
  <article
    class="prose
  prose-h1:font-bold prose-h1:text-2xl
  prose-a:text-blue-600 prose-p:text-justify"
  >
    <slot />
  </article>
</main>
